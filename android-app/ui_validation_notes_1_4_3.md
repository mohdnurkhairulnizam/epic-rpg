# Android 1.4.3 NFC Position Adjustment Validation

Only the NFC Scan Card position changed. The card is now anchored immediately below the unchanged header in the right-hand open space marked in the latest layout reference. It remains above the Dashboard content and away from the bottom navigation.

The header retains the corrected Android 1.4.1 baseline: original `20px` padding, `104px` height, brand-row composition, title position, icon, ore badge, and material styling. The Dashboard card layout and bottom navigation were not modified.

Visual inspection of the fresh local preview confirmed the Scan Card sits in the marked right-side dashboard-title zone while the rest of the page composition remains unchanged.
